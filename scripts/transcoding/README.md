These are scripts that can be used to automate the transcription of your video library from any format into H.264+AAC MKV containers + separate srt file.

Input:
- Source folder
- Bit rate (defaults to 2Mb/s)
- Destination host
- Destination folder

Supports SFTP or scp. 